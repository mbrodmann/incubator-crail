apiVersion: apps/v1
kind: Deployment
metadata:
  name: crail-workload
  namespace: crail
spec:
  replicas: 1
  selector:
    matchLabels:
      run: crail-workload
  template:
    metadata:
      labels:
        run: crail-workload
    spec:
      containers:
      - name: crail-relocator
        image: crail-relocator
        imagePullPolicy: Never
        workingDir: /crail-S3
        resources:
          requests:
            cpu: "100m"
            memory: "100Mi"
        ports:
        - name: ns
          containerPort: 8765
        volumeMounts:
        - mountPath: /root/.kube
          name: kubeconfig-volume
        env:
         - name: NAMENODE_HOST
           value: crail-nameserver
         - name: INTERFACE
           value: eth0
        securityContext:
           readOnlyRootFilesystem: false
        command: ['/bin/bash']
        args: ["-c", "collectl --export lexpr | tee collectl.txt & bash crail-s3.sh run > workload.log"]
      volumes:
      - name: kubeconfig-volume
        secret:
          secretName: kubeconfig-secret
      nodeSelector:
        beta.kubernetes.io/os: linux

---
apiVersion: v1
kind: Service
metadata:
  name: crail-workload
  labels:
    run: crail-workload
  namespace: crail
spec:
  clusterIP: 10.109.169.161
  ports:
  - port: 8765
    targetPort: 8765
  selector:
    run: crail-workload

